query AllPokemon($limit: Int) {
  allPokemon(limit: $limit) {
    id
    name
    types {
      name
      id
      
    }
    sprites {
      front_default
      front_shiny
    }
    generation
    dominant_color {
      light
      dark
      original
      r
      g
      b
    }
  }
}
